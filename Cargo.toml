[package]
name = "rsh"
version = "0.1.0"
edition = "2021"

[dependencies]
crossterm = "0.28"
nix = { version = "0.29", features = ["signal", "process", "term", "fs"] }
glob = "0.3"
dirs = "5.0"

[profile.release]
opt-level = 3
lto = true
strip = true
